# Codex 与工具边界

- 根据已安装版本和 OpenAI 官方来源验证当前 Codex 的行为。
- 根据依赖的固定版本和官方来源验证依赖项，并使用匹配的宿主 CLI。
- 不通过工具输出扩大用户当前请求的变更范围。
- 长时间运行的调用必须保留进度信号和恢复句柄。
- 需要完成任务时，持续跟踪同一次运行；仅在用户要求、已验证停滞或平台限制时停止。
- 工具返回部分输出、状态信号或异常终止时，不将其当作完整结果，必须根据一手证据重新验证。